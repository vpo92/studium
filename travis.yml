dist: trusty
sudo: required

language: node_js
node_js:
  - "iojs"
  - "8"

addons:
  sonarcloud:
    organization: "vpo92-github"

jdk:
  - oraclejdk8
script:
  - npm run-script build
  - sonar-scanner
cache:
  directories:
    - '$HOME/.sonar/cache'
